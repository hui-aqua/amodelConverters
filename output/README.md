# Output Directory

This directory stores generated Blender scene files (`.blend`), render still images (`.png`), simulation cache data, runtime logs (`.build.log`), and desktop GUI session settings (`gui-settings.ini`).

All generated output files in this folder are intentionally excluded from version control via `.gitignore`.
